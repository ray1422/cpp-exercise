#include <bits/stdc++.h>
using namespace std;
 int main(){
    int n;
    cin >> n;
    for(int i = 0 ; i < n; i++) {
        int m;
        cin >> m;
        int cars[m];
        for(int j = 0 ; j < m j++) {
            cin >> cars[j];
        }

        int max_l = 0;

        for(int j = 0 ; j < m; j++) {
            int tmp_l = 0;
            // the heavier, the more front.
            unsigned long long int my_begin = 0, my_end = -1;
            for(int k = j ; k < m; k++) {
                if (cars[k] <= )

            }
        }
    }
 }
